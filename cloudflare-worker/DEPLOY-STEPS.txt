#!/bin/bash
# ===============================================================
# DEPLOY THE CLOUDFLARE WORKER — directory.grasshopperlocal.com
# Run this after you get the Cloudflare API token from Ryan
# ===============================================================

echo "═══════════════════════════════════════════"
echo "  STEP 1: Authenticate with Cloudflare"
echo "═══════════════════════════════════════════"
echo ""
echo "Ryan will provide an API token. Set it like this:"
echo "  wrangler login  —or—"
echo "  export CLOUDFLARE_API_TOKEN='<token>'"
echo ""

echo "═══════════════════════════════════════════"
echo "  STEP 2: Create KV Namespace"
echo "═══════════════════════════════════════════"
echo ""
echo "Run:"
echo "  wrangler kv:namespace create LEAD_KV"
echo ""
echo "Copy the returned ID and paste it into wrangler.toml:"
echo "  id = \"<the-namespace-id>\""
echo ""

echo "═══════════════════════════════════════════"
echo "  STEP 3: Set Secrets (wrangler secret put)"
echo "═══════════════════════════════════════════"
echo ""
echo "For each secret below, run:"
echo "  wrangler secret put <NAME>"
echo ""
echo "Secrets needed:"
echo "  TELNYX_API_KEY     — from therapy-laptop/work computer"
echo "  TELNYX_PHONE       — Telnyx phone number (format: +13195551234)"
echo "  SENDGRID_API_KEY   — from SendGrid free tier account"
echo "  STRIPE_SECRET_KEY  — from VPS vault at /etc/young-grasshopper/env"
echo "  STATS_TOKEN        — a random string (generate with: openssl rand -hex 16)"
echo ""

echo "═══════════════════════════════════════════"
echo "  STEP 4: Deploy"
echo "═══════════════════════════════════════════"
echo ""
echo "From the cloudflare-worker/ directory:"
echo "  cd /c/Users/R/projects/iowa-directory-repo/cloudflare-worker"
echo "  wrangler deploy"
echo ""
echo "Worker will be live at:"
echo "  https://iowa-directory-leads.workers.dev"
echo ""

echo "═══════════════════════════════════════════"
echo "  STEP 5: Verify"
echo "═══════════════════════════════════════════"
echo ""
echo "Test the stats endpoint (should return 401 without token):"
echo "  curl https://iowa-directory-leads.workers.dev/api/stats"
echo ""
echo "Test with token:"
echo "  curl -H 'Authorization: Bearer <STATS_TOKEN>' \\"
echo "    https://iowa-directory-leads.workers.dev/api/stats"
echo ""
echo "Test a lead submission:"
echo '  curl -X POST https://iowa-directory-leads.workers.dev/api/lead \\'
echo '    -d "name=Test+User&phone=555-0000&city=iowa-city&category=plumber"'
echo ""
echo "═══════════════════════════════════════════"
echo "  DONE — Lead capture is live!"
echo "═══════════════════════════════════════════"
